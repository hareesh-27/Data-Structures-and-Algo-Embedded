## Overview

This file contains the defenition of device drivers and why we need it.

It shows a small example with a LED blink program which we already wrote to give an overview of application layer and hardware layer.

The example demonstrates how normal register-level code can be converted into a device driver structure.

In the next steps, we are going to implement the same concept for GPIO.
